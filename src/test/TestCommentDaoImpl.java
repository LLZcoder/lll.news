package test;

import implement.CommentDaoImpl;
import valueBean.Comment;

public class TestCommentDaoImpl {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		/* 添加		
		CommentDaoImpl cdi = new CommentDaoImpl();
		
		Comment comment = new Comment();
		comment.setNewsid(1);
		comment.setContent("asdfafasdf嗡嗡嗡");
		comment.setUid(2);
		
		if( cdi.addComment(comment) == true)
			System.out.println("添加评论成功");
		else
			System.out.println("添加评论失败");
		 */
	
/*			删除
		CommentDaoImpl cdi = new CommentDaoImpl();
		int id = 2; //获取要删除的那条评论的ID
		if( cdi.delComment(id) == true)
			System.out.println("comid="+id+"的评论以删除" );
		else
			System.out.println("comid="+id+"的评论未未未删除" );
	*/	
		
		
	}

}
